version: '3.8'

services:
  shop_app:
    build:
      context: .  
      dockerfile: Dockerfile
    
    ports:
      - "8080:80"
    
    depends_on:
      postgres_db:
        condition: service_healthy

    command: /bin/bash -c "dotnet Shop.dll"
      
    environment:
      - ConnectionStrings__DefaultConnection=Host=postgres_db;Port=5432;Database=Sweet_Shop;Username=postgres;Password=12345
      - ASPNETCORE_ENVIRONMENT=Production
      
    restart: on-failure
    
  postgres_db:
    image: postgres:15-alpine
    ports:
      - "5432:5432" 
    environment:
      POSTGRES_DB: Sweet_Shop
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 12345
    volumes:
      - postgres_data:/var/lib/postgresql/data
      
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d Sweet_Shop"]
      interval: 5s  
      timeout: 5s  
      retries: 10  
      start_period: 10s 
      
    restart: always

volumes:
  postgres_data: